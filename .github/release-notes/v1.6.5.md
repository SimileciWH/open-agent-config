## ✨ Highlights

- **Web mode now requires a token** — other people on the same shared machine (e.g. a cluster login node) can no longer open your Open Agent Config. The token is saved locally, so you only need it once. Opt out with `oac serve --no-token`.
- **Update dialog** — its text is now selectable, and it follows your app language.

---
