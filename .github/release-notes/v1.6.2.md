## ✨ Highlights

- **Name your nodes** — `oac serve --name <label>` (defaults to the machine hostname) now labels each node in the web sidebar and the browser tab title. When you point multiple browser tabs at different remote machines, you can tell them apart at a glance.
