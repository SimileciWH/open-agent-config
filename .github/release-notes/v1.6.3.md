## ✨ Highlights

- **Linux ARM64 (web mode)** — the `oac` CLI now runs on aarch64 Linux via a new `oac-linux-arm64` build, so web mode (`oac serve`) works on ARM servers.
- **More accurate source attribution** — skills and plugins installed through a manifest (the `skills` CLI, marketplaces) now show their real upstream source, and a symlinked skill is attributed to its origin rather than the repo it happens to sit in.

---
