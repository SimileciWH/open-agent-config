name: PR Checks

on:
  pull_request:
    branches: [main]

jobs:
  test:
    # Pinned to macos-15 to match the release build env (release.yml) and avoid
    # the macos-latest -> macos-26 toolchain jump breaking PR builds mid-rollout.
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Rust cache
        uses: swatinem/rust-cache@v2

      - name: Install frontend dependencies
        run: npm ci

      - name: Validate release channel
        run: npm run check:release-channel

      - name: Validate OAC identity
        run: npm run check:oac-identity

      - name: Frontend tests
        run: npm test

      - name: Build frontend
        run: npm run build

      - name: Rust tests
        run: cargo test --workspace
